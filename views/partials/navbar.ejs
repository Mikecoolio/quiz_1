<nav class="navbar navbar-light bg-light">
    <div class="container-fluid">
      <img class="card-img-top" src="/public/css/images/chicken.svg" alt="Chicken Logo"/>
      <form action="/new_cluck" class="d-flex">
        <button class="btn btn-outline-success" type="submit" >Cluck!</button>
      </form>
      <form action="/sign_in" class="d-flex">
        <% if (username) { %>
          <span><%- username %> </span>
      </form>
      <form action="/sign_out" class="d-flex">
        <button class="btn btn-outline-success" type="submit">Sign out</button>
      </form>
        <% } else { %>
      <form action="/sign_in" class="d-flex">
        <button class="btn btn-outline-success" type="submit">Sign in</button>
      </form>
        <% } %> 
      </form>
    </div>
</nav>